import styled from 'styled-components'
import {ReactComponent as NoiseLayer} from './NoiseLayer.svg'

export const NoiseLayrer = styled(NoiseLayer)`
  height: auto;
  max-width: 760px;
  width: 100%;
  mix-blend-mode: overlay;
`
